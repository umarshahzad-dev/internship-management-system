import { useState } from 'react'
import { useMutation, useQuery, useQueryClient } from '@tanstack/react-query'
import { Button, DataToolbar, FileUpload, Input, LoadingState, PageHeader, Panel, Pagination, Select, Table } from '../../../components/ui'
import { api } from '../../../lib/api'
import { useTableUrlState } from '../../shared/hooks/useTableUrlState'

export function AdminOperationsPage({ mode }: { mode: 'users' | 'departments' }) {
  const client = useQueryClient(); const table = useTableUrlState(); const [departmentForm, setDepartmentForm] = useState({ name: '', facultyName: '' }); const [userForm, setUserForm] = useState({ email: '', password: '', role: 'STUDENT', firstName: '', lastName: '', studentNumber: '', departmentId: '' }); const [message, setMessage] = useState('')
  const list = useQuery({ queryKey: [mode], queryFn: async () => (await api.get<Array<Record<string, unknown>>>(`/${mode}`)).data }); const allRows = list.data ?? []; const filteredRows = allRows.filter((row) => Object.values(row).some((value) => String(value ?? '').toLowerCase().includes(table.search.toLowerCase()))); const rows = [...filteredRows].sort((left, right) => { if (!table.sort) return 0; const leftValue = String(left[table.sort] ?? '').toLocaleLowerCase(); const rightValue = String(right[table.sort] ?? '').toLocaleLowerCase(); return leftValue.localeCompare(rightValue, 'tr') }); const totalPages = Math.max(1, Math.ceil(rows.length / table.pageSize)); const pageRows = rows.slice((table.page - 1) * table.pageSize, table.page * table.pageSize)
  const create = useMutation({ mutationFn: () => api.post(`/${mode}`, mode === 'departments' ? departmentForm : { ...userForm, departmentId: userForm.departmentId || undefined, studentNumber: userForm.studentNumber || undefined }), onSuccess: async () => { setMessage('Kayıt oluşturuldu.'); await client.invalidateQueries({ queryKey: [mode] }) } }); const importUsers = useMutation({ mutationFn: (file: File) => { const data = new FormData(); data.append('file', file); return api.post('/users/import', data) }, onSuccess: () => setMessage('CSV içe aktarma tamamlandı.') })
  const sortOptions = mode === 'departments' ? [{ value: 'name', label: 'Bölüm adı' }, { value: 'facultyName', label: 'Fakülte adı' }] : [{ value: 'email', label: 'E-posta' }, { value: 'role', label: 'Rol' }, { value: 'lastName', label: 'Soyad' }]
  return <><PageHeader title={mode === 'users' ? 'Kullanıcı yönetimi' : 'Bölüm yönetimi'} description="Yönetim işlemlerini formlar ve URL durumlu tablolarla yürütün." /><div className="grid gap-4 lg:grid-cols-[1fr_1.5fr]"><Panel title={mode === 'users' ? 'Kullanıcı oluştur' : 'Bölüm oluştur'}>{mode === 'departments' ? <><Input label="Bölüm adı" value={departmentForm.name} onChange={(event) => setDepartmentForm({ ...departmentForm, name: event.target.value })} /><Input className="mt-3" label="Fakülte adı" value={departmentForm.facultyName} onChange={(event) => setDepartmentForm({ ...departmentForm, facultyName: event.target.value })} /></> : <div className="grid gap-3 sm:grid-cols-2"><Input label="E-posta" type="email" value={userForm.email} onChange={(event) => setUserForm({ ...userForm, email: event.target.value })} /><Input label="Geçici şifre" type="password" value={userForm.password} onChange={(event) => setUserForm({ ...userForm, password: event.target.value })} /><Input label="Ad" value={userForm.firstName} onChange={(event) => setUserForm({ ...userForm, firstName: event.target.value })} /><Input label="Soyad" value={userForm.lastName} onChange={(event) => setUserForm({ ...userForm, lastName: event.target.value })} /><Select label="Rol" value={userForm.role} onChange={(event) => setUserForm({ ...userForm, role: event.target.value })} options={['STUDENT', 'ACADEMIC', 'ADMINISTRATIVE', 'ADMIN'].map((role) => ({ value: role, label: role }))} /><Input label="Öğrenci numarası (isteğe bağlı)" value={userForm.studentNumber} onChange={(event) => setUserForm({ ...userForm, studentNumber: event.target.value })} /><Input label="Bölüm ID (isteğe bağlı)" value={userForm.departmentId} onChange={(event) => setUserForm({ ...userForm, departmentId: event.target.value })} /></div>}<Button className="mt-4" loading={create.isPending} disabled={mode === 'users' && (!userForm.email || !userForm.password || !userForm.firstName || !userForm.lastName)} onClick={() => void create.mutateAsync()}>Kaydet</Button>{message ? <p className="mt-3 text-sm text-navy" role="status">{message}</p> : null}</Panel>{mode === 'users' ? <Panel title="CSV içe aktar"><FileUpload label="Kullanıcı CSV dosyası" accept=".csv,text/csv" onFilesSelected={(files) => { const file = files[0]; if (file) void importUsers.mutateAsync(file) }} /></Panel> : null}</div><Panel className="mt-4" title="Kayıtlar"><DataToolbar search={{ value: table.search, onChange: (value) => table.update({ search: value }), placeholder: 'Kayıtlarda ara' }} filters={<Select label="Sıralama" aria-label="Sıralama" value={table.sort} onChange={(event) => table.update({ sort: event.target.value })} options={[{ value: '', label: 'Sıralama yok' }, ...sortOptions]} />} />{list.isLoading ? <LoadingState label="Kayıtlar yükleniyor" /> : list.isError ? <p className="text-sm text-red" role="alert">Kayıtlar alınamadı.</p> : rows.length === 0 ? <p className="text-sm text-gray-500">{table.search ? 'Sonuç bulunamadı.' : 'Kayıt bulunamadı.'}</p> : <><Table caption={mode} columns={(mode === 'departments' ? [{ key: 'name', header: 'Bölüm' }, { key: 'facultyName', header: 'Fakülte' }] : [{ key: 'email', header: 'E-posta' }, { key: 'role', header: 'Rol' }, { key: 'firstName', header: 'Ad' }, { key: 'lastName', header: 'Soyad' }])} data={pageRows} rowKey={(row, index) => String(row.id ?? index)} /><Pagination className="mt-4 justify-end" page={Math.min(table.page, totalPages)} totalPages={totalPages} onPageChange={(page) => table.update({ page })} /></>}</Panel></>
}
